#include <bits/stdc++.h>
using namespace std;
struct node
{
    int data;
    struct node *next;
};
struct node *head = NULL;

void ins(int q)
{
    struct node *p;
    p = (struct node *)malloc(sizeof(struct node));
    p->data=q
    p->next=NULL:
    head=p;
}
void insert()
int main()
{
    insert(10);
    
    print();
}